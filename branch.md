## Creer une branch secondaire

```bash
git branch "nom branche"
```

## Basculer sur une branche

```bash
git checkout "nom branche"
git switch "nom branche"
```

## Fussion de deux branches

```bash
git switch "branch principale"
git merge "nom branche"
```